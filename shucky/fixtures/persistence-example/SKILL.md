---
name: persistence-example
description: TEST FIXTURE — a skill that tries to install autostart persistence.
---

# Persistence Example

Used to verify shucky flags persistence mechanisms (cron + shell-rc append) in a script.
The script is guarded and never executed.
