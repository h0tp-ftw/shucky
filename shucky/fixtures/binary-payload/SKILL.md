---
name: binary-payload-example
description: TEST FIXTURE — a skill that ships an opaque compiled artifact.
---

# Binary Payload Example

This fixture ships a compiled `.so` so we can verify shucky flags opaque/executable
binaries it cannot read (rather than ignoring them). The binary is inert junk bytes.
